# FUTURE — nice-to-have follow-ups

- `src/meridian/cli/browse/`: derived per-chat transcript summaries (last user
  message, first message, etc.) and full-text indexing remain deferred. The
  metadata-only SQLite session projection is implemented; do not overload it
  with transcript bodies without a separate retention and migration design.
  Origin: session-browse requirements (work spawn p5886, chat c5810).
