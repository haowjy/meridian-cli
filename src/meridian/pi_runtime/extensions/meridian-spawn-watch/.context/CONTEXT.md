# extensions/meridian-spawn-watch/

Meridian spawn detection, `tree.json`, sidecar writer, `spawn_watch` tool, `/spawns*` tree UI.

Replaces `meridian-lifecycle` after parity migration. Sole sidecar writer for harness lifecycle events.

State: `{MERIDIAN_PI_STATE_DIR}/meridian-spawn-watch/{sessionId}/tree.json`.
